{
  "services": [
    {
      "domain": "manta.{{REGION}}.{{DNS_DOMAIN}}",
      "external": true,
      "http": true,
      "https": true,
      "internalHttp": true
    }
  ],
  "adminIp": "{{MANTA_IP}}",
  "externalIp": "{{EXTERNAL_IP}}",
  "zookeeper": {
    "connectTimeout": 1000,
    "servers": [
      {{#ZK_SERVERS}}
      {
        "host": "{{host}}",
        "port": {{port}}
      }{{^last}}, {{/last}}
      {{/ZK_SERVERS}}
    ],
    "timeout": 60000
  }
}
