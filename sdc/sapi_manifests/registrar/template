{
  "registration": {
    "domain": "{{{SERVICE_DOMAIN}}}",
    "lbServices": [
      {{#LB_SERVICES}}
      "{{domain}}"{{^last}}, {{/last}}
      {{/LB_SERVICES}}
    ],
    "type": "load_balancer",
    "service": {
      "type": "service",
      "service": {
        "srvce": "_{{{SERVICE_NAME}}}",
        "proto": "_tcp",
        "port": 80
      },
      "ttl": 60
    },
    "ttl": 30
  },

  "zookeeper": {
    "servers": [
      {{#ZK_SERVERS}}
        {
          "host": "{{host}}",
          "port": {{port}}
        }{{^last}}, {{/last}}
      {{/ZK_SERVERS}}
    ],
    "timeout": 60000
  }
}
